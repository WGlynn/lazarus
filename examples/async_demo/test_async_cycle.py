pytest wrapper (CI note in the spec). 8 tests, all PASSING under `pytest -k async_cycle` on a bare checkout (stdlib + tomli only). Steps (b)-(f) are in-process and fully deterministic; step (a) uses a MOCKED _spawn_detached (monkeypatch on lazarus_async_launch._spawn_detached) so no real detached child runs on locked-down CI -- it keeps only the launcher's fast-return + spool-file + reads-work-unit-from-FILE contract. Tests: test_async_cycle_a_launcher_spool_and_fast_return, _b_runner_writes_pending, _b_dedup_second_run_adds_zero, _c_inject_emits_and_consumes, _d_second_inject_is_silent, _e_v1_sync_demo_still_passes, _e_additive_defaults_plain_v1_config_is_sync, _f_stub_parity. Uses the same fixture/oracle/extractor as run_async_demo.py.