The v1 demo config PLUS an [async] table. [corpus] path='../demo/corpus' and [ledger] path='../demo/.lazarus/ledger.jsonl' point at the SHARED v1 demo paths so the oracle is byte-identical (2 surfaced, 1 declined). [async] mode='async', pending_path='.lazarus/pending.jsonl', spool_dir='.lazarus/async', stub_judge=true (forces the offline deterministic judge -> no API key). [async.pregate] enabled=false. VERIFIED: load_config resolves corpus/ledger to the shared demo dir and pending/spool demo-local; async_enabled True, mode 'async'.