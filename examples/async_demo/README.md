House-style README matching examples/demo/README.md. Documents: the one command + expected 'ASYNC DEMO PASSED' tail; the file table; the four transport pieces (launcher/runner/pending queue/injection hook) and their on-disk locations; all six asserted steps (a)-(f) mapped to contract requirements; the 'one extractor, one oracle' (D-3) explanation for why (a)/(b) agree and why (b) wipes the ledger; the pytest usage; and the 'why a stub judge' rationale + how to swap in the real Claude judge. Reuses the v1 corpus/fixture verbatim (zero new corpus).