NEW contract-canonical-named alias (spec §3 / verify spec name the launcher hooks/async_launcher.py; the on-disk impl is hooks/lazarus_async_launch.py). Thin executable that bootstraps the hooks dir onto sys.path and delegates to lazarus_async_launch.main(), so piping a PostToolUse/Stop event into `python hooks/async_launcher.py` runs the exact same non-blocking launcher. The async demo's step (a) drives this path (VERIFIED: exit 0 in ~0.19s).