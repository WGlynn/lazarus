NEW contract-canonical-named alias (spec §4 / verify spec name the injection hook hooks/async_inject.py; the on-disk impl is hooks/lazarus_inject.py). Thin executable that bootstraps the hooks dir onto sys.path and delegates to lazarus_inject.main(), so piping a UserPromptSubmit event into `python hooks/async_inject.py` runs the exact same fail-SAFE inject hook (read unconsumed -> emit additionalContext with PROPOSALS framing -> mark consumed). The async demo's steps (c)/(d) drive this path (VERIFIED: inject#1 emits 2 findings + marks 2 CONSUMED; inject#2 silent).