NEW vendored offline stub judge -- the installed-fallback twin the runner's _load_stub_judge_fn imports (from .async_.stub_judge import stub_judge_fn) and step (f)'s anti-drift guard checks. It was referenced by src/lazarus_sonar/async_runner.py but did NOT exist on disk, so --stub from an installed wheel (and the launcher's stub-judge propagation) would have failed. Contents are a VERBATIM LOGIC COPY of examples/demo/stub_judge.py (same ALLOW allowlist, same would_change=True@0.9 / False@0.2 verdicts, same dict shape), importing Candidate from ..sonar. The contract's placeholder `from .stub_judge import stub_judge_fn` self-import is a doc stand-in; the real module must carry the logic so --stub works with no demo dir on the path. Parity with the demo stub is asserted by the demo step (f) and pytest test_f_stub_parity (both GREEN).